name: ARC smoke
on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: k3s-runners
    container:
      image: ubuntu:24.04
    steps:
      - run: |
          echo "hello from container job"
          uname -a
          id
